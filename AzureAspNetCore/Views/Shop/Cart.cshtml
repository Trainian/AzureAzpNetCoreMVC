
@{
    ViewBag.Title = "Корзина";
}

    <section id="cart_items">
        <div class="container">
            <div class="breadcrumbs">
                <ol class="breadcrumb">
                    <li><a href="#">Home</a></li>
                    <li class="active">Shopping Cart</li>
                </ol>
            </div>

            @await Html.PartialAsync("_CartInfo")
            
            @await Html.PartialAsync("_DoAction")

        </div>
    </section>
